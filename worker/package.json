{
  "name": "examgen-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker para ExamGen - Generador de Exámenes con Gemini",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=neutral --format=esm --outfile=dist/index.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@google/generative-ai": "^0.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
