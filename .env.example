# OpenRouter API Key
# NOTA: La API key ahora se gestiona en el Cloudflare Worker (server-side)
# Este archivo es solo para referencia. Los usuarios NO necesitan proporcionar su propia API key.
# Para desarrollo local del worker, configura la API key en worker/.dev.vars
VITE_OPENROUTER_API_KEY=your-api-key-here-if-needed
